name: Build Docker images

permissions:
  contents: read
  pull-requests: write

on:
  push:
    branches: ["main"]
  workflow_dispatch: {}

jobs:
  build-and-push-dockerfile-image:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      EXT_DL_URL_MARKDOWN: ${{ secrets.EXT_DL_URL_MARKDOWN }}
      EXT_DL_URL_ORIGINAL: ${{ secrets.EXT_DL_URL_ORIGINAL }}
      EXT_DL_URL_MARKDOWN_EXTERNAL: ${{ secrets.EXT_DL_URL_MARKDOWN_EXTERNAL }}
      EXT_DL_URL_ORIGINAL_EXTERNAL: ${{ secrets.EXT_DL_URL_ORIGINAL_EXTERNAL }}

    environment:
      name: production
      url: https://gallery.voidtales.win/

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: 🗑️ Remove download markers
        run: |
          rm -f src/content/photos/.downloads_synced
          rm -f public/images/original/.downloads_synced

      # Restore cache with last known key (from previous run)
      - name: 🔧 Restore downloaded files cache
        id: cache-restore
        uses: actions/cache@v4
        with:
          path: |
            src/content/photos
            public/images/original
          key: downloads-${{ runner.os }}-latest
          restore-keys: |
            downloads-${{ runner.os }}-

      - name: 📦 Install dependencies
        run: |
          npm install -g pnpm
          pnpm install --frozen-lockfile

      - name: 📥 Download files (if not cached)
        env:
          EXT_DL_URL_MARKDOWN: ${{ secrets.EXT_DL_URL_MARKDOWN }}
          EXT_DL_URL_ORIGINAL: ${{ secrets.EXT_DL_URL_ORIGINAL }}
          EXT_DL_URL_MARKDOWN_EXTERNAL: ${{ secrets.EXT_DL_URL_MARKDOWN_EXTERNAL }}
          EXT_DL_URL_ORIGINAL_EXTERNAL: ${{ secrets.EXT_DL_URL_ORIGINAL_EXTERNAL }}
        run: |
          pnpm run copy:md-files
          pnpm run copy:original-images

      # Calculate new cache key after download (hash of all files)
      - name: Calculate cache key (after download)
        id: cache-key-after
        run: |
          echo "key=$(find src/content/photos public/images/original -type f | sort | sha256sum | cut -d' ' -f1)" >> $GITHUB_OUTPUT

      # Save cache with new key (always overwrite "latest")
      - name: 🔧 Save downloaded files cache
        uses: actions/cache/save@v4
        with:
          path: |
            src/content/photos
            public/images/original
          key: downloads-${{ runner.os }}-latest
          # Optionally also save with hash key for debugging:
          # key: downloads-${{ runner.os }}-${{ steps.cache-key-after.outputs.key }}

      - name: 🗑️ Remove thumbnail marker
        run: rm -f .thumbs_generated

      - name: 🔧 Cache thumbnails
        uses: actions/cache@v4
        with:
          path: public/images/thumbs
          key: thumbs-${{ runner.os }}-${{ hashFiles('package.json', 'pnwm-lock.yaml', 'scripts/generate-thumbs.js', 'public/images/original/**') }}
          restore-keys: |
            thumbs-${{ runner.os }}-

      - name: 🖼️ Generate thumbnails (if not cached)
        run: |
          mkdir -p public/images/thumbs
          pnpm run gen:thumbs

      - name: 🔧 Save thumbnails cache
        uses: actions/cache/save@v4
        with:
          path: public/images/thumbs
          key: thumbs-${{ runner.os }}-${{ hashFiles('package.json', 'pnwm-lock.yaml', 'scripts/generate-thumbs.js', 'public/images/original/**') }}

      - name: 🔧 Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🔨 Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            inventory69/voidtales-gallery:latest
          platforms: linux/amd64
          build-args: |
            EXT_DL_URL_MARKDOWN=${{ env.EXT_DL_URL_MARKDOWN }}
            EXT_DL_URL_ORIGINAL=${{ env.EXT_DL_URL_ORIGINAL }}
            EXT_DL_URL_MARKDOWN_EXTERNAL=${{ env.EXT_DL_URL_MARKDOWN_EXTERNAL }}
            EXT_DL_URL_ORIGINAL_EXTERNAL=${{ env.EXT_DL_URL_ORIGINAL_EXTERNAL }}

      - name: 🚀 Trigger Dokploy Deployment
        run: |
          curl -X POST \
          -H "Authorization: Bearer ${{ secrets.DOKPLOY_API_TOKEN }}" \
          -H "Content-Type: application/json" \
          -d '{ "image_tag": "inventory69/voidtales-gallery:latest" }' \
          "${{ secrets.API_HOST }}/api/deploy/${{ secrets.DOKPLOY_APP_ID }}"
        env:
          DOKPLOY_API_TOKEN: ${{ secrets.DOKPLOY_API_TOKEN }}
          DOKPLOY_APP_ID: ${{ secrets.DOKPLOY_APP_ID }}